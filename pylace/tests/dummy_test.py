"""Tests the tester, no real test here."""


def test_placeholder() -> None:
    """
    Test the tester, not expected to fail.

    Tests the tes only exists to ensure that there is at least 1 test for `pytest`,
    and thus that it does not exit with a failure due to
    no Python tests existing.
    """
    pass
